import { useEffect } from 'react';
import { useLocation, useNavigate } from 'react-router-dom';

export function KeyboardShortcuts(){const navigate=useNavigate();const location=useLocation();useEffect(()=>{const onKey=(e:KeyboardEvent)=>{const el=e.target as HTMLElement|null;const typing=Boolean(el&&(el.tagName==='INPUT'||el.tagName==='TEXTAREA'||el.tagName==='SELECT'||el.isContentEditable));if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();if(location.pathname!=='/browse')window.dispatchEvent(new Event('salla-open-search'));return;}if(!typing&&e.key==='/'&&location.pathname!=='/browse'){e.preventDefault();window.dispatchEvent(new Event('salla-open-search'));return;}if(e.altKey&&e.key==='ArrowLeft'){e.preventDefault();navigate(-1);return;}if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='r'&&location.pathname==='/browse'){e.preventDefault();window.dispatchEvent(new Event('salla-refresh-frame'));}};window.addEventListener('keydown',onKey);return()=>window.removeEventListener('keydown',onKey)},[location.pathname,navigate]);return null}
